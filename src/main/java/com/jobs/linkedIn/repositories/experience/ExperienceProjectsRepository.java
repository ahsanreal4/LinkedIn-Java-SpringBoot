package com.jobs.linkedIn.repositories.experience;

import com.jobs.linkedIn.entities.experience.ExperienceProjects;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ExperienceProjectsRepository extends JpaRepository<ExperienceProjects, Long> {
}
